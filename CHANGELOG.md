# 更新日志

## 项目时间表 (Project Timetable)

### v0.3.0

- **打包成 Windows 桌面版**,双击即用,不需要装 Node、不需要开终端
- **数据改存磁盘上的 JSON 文件**,默认在「文档\项目时间表\」下,
  位置可以自己选(放进网盘目录就能同步)。每次改动自动写入,
  写法是先写临时文件再原子改名,写一半崩了也不会把好数据糊掉。
  升级时旧数据会自动迁进新文件,不用手工搬
- 顶栏显示最后保存时间,写盘失败会红字报警;另有「备份」按钮
  另存带时间戳的副本(保留最近 30 份)
- **子项目内部再分「子行」**:层级变成 项目 → 子项目 → 子行 → 任务。
  子行可新建、重命名、删除、拖动排序;任务可以跨子行拖动
- 子项目可**折叠成一条汇总条**:跨度取最早到最晚,进度按工期加权;
  进出这一摊的连线自动并到汇总条上
- **连接线可以删了**:线上单击选中、按 Del 删除;
  任务侧板里也列出「上级任务 / 下级任务」,逐条断开
- 任务可标**固定日期**:上游改期时它不动,联动到它就截断。
  适合交付、验收这类已经对外承诺的节点
- 任务右键菜单加了**进度档位**,改进度不用再开侧板
- 顶栏加**「新任务」块**:点一下就建,或拖到某条子行的某个日期上放置
- 顶栏加**「全览」**:一屏放下从最早到最晚的全部内容
- 修正任务被拖到项目起止日期之外时**滚动条够不着、任务像凭空消失**的问题
- 侧边栏区域可右键或点加号**新建项目**
- 换了应用图标

### v0.2.1

- 泳道式的多项目时间表:一行是一条工作流,同一行里横向排列前后衔接的任务,
  只有真正并行的工作才另起一行
- 侧边栏分成 区域 → 执行中/未开始/已完成 → 项目 三级,可以整条收起
- 任务块可横向拖动改期、拖边缘改工期、纵向拖到别的泳道;泳道本身也能拖动排序
- 任务之间可以拉连接线表达先后依赖(支持跨泳道),拖上游任务时下游沿链路一起平移
- 连接线平时隐藏,鼠标停在某个任务上才浮现它前后那两条;右键"孤立此链路"可只看一条链
- 数据存在本机,支持 JSON 导入导出

## 吸附制冷温控采集软件 (Sorption Cooler Control)

### v0.9.7

- **修正:制冷机保持到一半,热开关的加热会被自己关掉。** 热开关在保持阶段本来就要
  一直加热到液氦耗尽 —— 十几个小时都正常,而加热参数里「最长连续加热」默认 120 分钟,
  于是每到两小时就把它掐掉一次:热开关断开、吸附泵浮起来回温放气、抽气停止,冷量当场断掉。
  现在**制冷机程序运行期间热开关不受这一条约束** —— 它该开多久由物理过程决定,
  不是由挂钟决定。吸附泵不在此列(它的加热阶段有明确终点)。
  超温上限和传感器断线保护一条都没动
- 顺带修正:这条限制跳闸之后**不锁存** —— 关掉输出的同时把计时起点也清了,
  下一个周期又打开、重新计时。也就是说它既拦不住真正的失控,界面和日志里还一个字都没有。
  现在跳了就锁住,卡片显示「加热超时」,日志里写清楚怎么恢复
- 吸附泵的「最长连续加热」如果比「解吸升温 + 冷凝时长」还短,点「开始测试」时就会拦下来
- **修正:虚化(毛玻璃)背景底下的按钮照样点得动**,点一下主界面还会盖到对话框前面,
  看着像是把对话框关掉了。现在和一般软件一样:对话框没处理完,底下点不动

### v0.9.6

- **支持连续吸附制冷机(两台模块轮换)**。通道角色扩成:4K冷台和1K平台两模块共用,
  吸附泵/热开关/蒸发器各分1号2号。测试模式增加「连续测试」:一个模块的蒸发器到 1K
  之后,等 4K 冷台把解吸压上去的热量散掉(还可以再设几小时延时),就启动另一个模块
  再生,当前模块继续保持,冷量不中断。等不到 4K 回落会报「连续切换失败」并停止轮换 ——
  但当前模块继续保持,这一轮的冷量不浪费
- 老配置里的「吸附泵 / 热开关 / 1K冷头」会自动变成 1 号模块的对应角色,不用重新指定
- **加热配错仪器时,点「开始测试」之前就会被拦下来**。以前是每个采样周期报一次错、
  加热永远开不起来,对着刷屏的日志根本看不出是配置问题
- 右上角的「加热控制」改成**加热总开关**,只有「关闭全部」和「启用」两档。
  点「关闭全部」会把制冷机程序一并停止
- **关闭软件时会先把所有加热关掉再退出**
- 加热参数窗口里,被制冷机程序接管的那一路会写明:目标温度和控制模式以「程序配置」
  里的为准,而**加热档位仍然按加热参数里设的**

### v0.9.5

- **修正:曲线的时间窗按钮和"重置视图"有时完全不起作用**,X 轴一直卡在原来的范围。
  触发路径是:取消勾选"X轴自动" → 缩放或拖动过曲线 → 再把勾勾回来。
  (v0.9.2 改曲线绘制方式时带出来的:set_xlim 会把自动缩放永久关掉)
- **修正:X 轴刻度落在"4小时48分"这种位置**上而不是整点
- **修正:加热卡片上,挂在 350 上的加热一律显示成"GPP-CHx"**,回读一律是"-- V / -- A"。
  这是 v0.8 把仪器改成清单之后留下的 —— 卡片还在拿仪器编号去比对旧的型号名
- 加热卡片改版:通道名用你自己起的名字(不再是 t1:A 这种内部写法),输出口写成
  OUT1 / CH2,回读按仪器类型分行;鼠标停上去能看到完整的仪器名。面板也放宽了,
  文字放不下会折行而不是被切掉
- 采集进行中"＋加一路/删一路"变灰时,鼠标停上去会说明为什么
- **修正:变化率面板显示不全** —— 第三列(可选统计窗口)整列被压掉,而且左右拉不动。
  现在三列都装得下,8 个通道在 1366×768 上一眼看全;装不下时两个方向都能拉
- **修正:主窗口有可能比屏幕还宽** —— 通道名一长(接两台测温仪时默认名字会带上
  仪器名),最小宽度会被顶到两千多像素,窗口再也缩不回来。现在无论通道名多长、
  接几台仪器,都能缩回 1366

### v0.9.4

- **支持国产温控仪:赋同量子 PhotempC08 / PhotempC01**。C08 是 8 路测温 + 4 路加热,
  C01 是 1 路 + 1 路,两台都能用仪器自带的闭环 PID 控温。接法是 USB 虚拟串口
  (要装 CH340 驱动)或 RS-485,固定 115200 波特率,界面上不用填
- 这两台一条指令就能读回全部通道,所以一轮采样只发 2 条,通道多也不拖慢
- 加热档位、升温限速这些按型号自动切换:Photemp 只有低/中/高三档,协议里也没有
  斜坡指令,做不到的项界面上直接置灰,不摆在那里让人误填
- CSV 里加热那几列对这两台记的是**输出电压**而不是百分比 —— 它读不到加热百分比

### v0.9.3

- **采样快了一个数量级**。一轮里要问的读数现在串成一条指令发给仪器。
  Lake Shore 每条查询的往返要一两百毫秒,以前 4 个通道一轮要发 13 条、耗时 3 秒多
  (超过设定的 2 秒周期),8 通道要 5 秒。现在 4 通道和 8 通道都是 0.24 秒。
  仪器不支持串联的话会自动退回一条一条问
- 关着的加热通道不再每轮去问一次输出百分比 —— 档位是关的,输出必然是 0
- **Lake Shore 350 增加 USB 连接方式**(后面板那个 USB 口就是虚拟串口,不用转接线)
- **新增 Lake Shore 372 支持(测温)**:以太网和 USB 都行,控制输入 A 加 CH1~CH16。
  它只有一台交流电桥轮流扫 16 路,读数不是实时的 —— 连上之后会检查自动扫描是不是
  开着的(关掉的话别的通道读到的全是旧值),并算出扫完一圈要多久
- 采样间隔增加 **15 s / 30 s / 1 min / 2 min** 几档,给 372 这类扫描式仪器用;
  采样比扫描一圈还快时会明确提示

### v0.9.2

- **双击打开后主界面不再躲在资源管理器后面**。主界面是趁启动画面还占着前台的时候建出来的,
  所以排在了它后面;现在窗口显示出来之后会主动抢一次前台
- **界面不再"未响应"**:凡是要和仪器通信的操作(连接测试、开始采集时的连接、仪器配置的
  读取和写入)全部挪到后台。等待窗上写着正在做第几项、已经等了多久,等不下去还能点"不等了"
- **修正串口每条指令都要等满超时**:仪器的输出终止符只有 CR 时,以前会一直等到超时才罢休
  —— 2401 接 USB 转串口时点一次"配置"要卡几十秒就是这个原因。现在收到回复立刻返回
- **曲线重绘提速**:曲线对象不再每秒推倒重建,取数、抽稀、变化率、时间轴换算都重写过;
  窗口最小化时不重绘;重绘频率跟着采样间隔走。每次重绘的计算量从约 90 ms 降到约 3 ms
- 仪器配置窗口:写入之后的自动回读不再把刚发出去的指令记录清掉

### v0.9.1

- **修正**:点"配置 → 仪器配置"会报未预期错误(上一版改函数签名时漏了调用方)
- 通道设置窗口里的温度和传感器原始值现在**实时刷新**,换来源仪器/输入时读数也跟着换
  —— 正好用来确认这一路选对了没有
- 打开对话框时主界面会**虚化**,窗口淡入淡出
- 启动画面修掉了文字重叠,背景改成纯色,不再有渐变留下的边

### v0.9.0

- 双击之后**立刻出现启动画面** —— 以前要等几秒才有第一个窗口,容易让人以为没点上而再双击
- **只允许开一个实例**:再双击会把已经开着的窗口调到前面(两份会抢同一个串口、写同一个 CSV)
- 通道数增加 **12 / 16** 两档,还可以自定义(1~16);卡片按通道数自动排布,始终不超两行
- **加热路数可以自己加减**(面板下方"＋ 加一路 / 删一路",最多 8 路),默认给到 5 路
- 默认采样间隔改成 **2 秒**

### v0.8.0

- 仪器改成一份**可增删的清单**:同型号可以接多台(比如两台 350),在"连接设置"里点
  "添加仪器"即可,每台有自己的名字和连接参数
- 凡是要选仪器的地方(通道来源、加热输出、仪器配置),列的都是清单里**真有的仪器**,
  不再是把支持的型号都摆出来;默认就一台
- "仪器配置"改成先选哪一台再配 —— 224/325 同样能配传感器类型、量程、标定曲线这些,
  以前只能配 350 是实现限制,不是仪器的限制
- 350 的 PID 现在要求被控通道就在同一台 350 上(两台 350 时这一点很关键)
- 界面做了一轮视觉整理:统一圆角与间距、按钮有按下反馈、弱化边框
- ⚠ 通道键格式变了,旧的通道设置和加热配置需要重新设一遍

### v0.7.6

- 采集线程反复报的同一条错误不再刷屏:只在第一次显示,之后每 5 分钟汇总一次出现了多少次
  (没接传感器的通道、没有加热输出的仪器,以前每个采样周期都要报一遍)
- 修正加热通道控着一路界面上没显示的温度时,曲线里凭空多一条目标温度虚线的问题
- 通道设置和加热参数里,没在用的仪器会置灰并标出状态,不会再从一长串用不到的输入里误选
- 连上仪器后核对型号,连错了(比如两个小节填了同一个 IP)会明确警告

### v0.7.5

- 新增内置的**使用说明**:菜单 → 使用说明,或者直接按 <kbd>F1</kbd>。分成快速上手 / 连接仪器 /
  采集与数据 / 温度通道 / 曲线与变化率 / 加热控制 / 制冷机程序 / 常见问题八章,
  还能另存成网页方便打印
- 修正任务栏上不显示软件图标的问题

### v0.7.4

- 修正笔记本上对话框超出屏幕、底部"保存"按钮点不到的问题:所有窗口都按屏幕可用高度
  自动收窄,正文改成可滚动,按钮固定在窗口底部
- 连接设置里温度测量 / 直流电源 / 精密电源三组可以各自折叠,还有"全部折叠"按钮

### v0.7.3

- 连接助手升到 v0.2:连不上时按报错自动判断问题出在电脑还是仪器

### v0.7.2

- 主软件的“关于”里也加上了检查更新(之前只有连接助手有)

### v0.7.1

- 检查更新的服务地址调整
- 英文命名统一改用 sorption cooler(极低温领域的通用叫法)

### v0.7

- 新增独立的**连接助手**小软件:三个指示灯看连接状态,分步引导连接仪器,能直接改电脑网卡 IP,连好后设置自动传给主软件
- 通道名称、颜色、角色、显示开关、通道数、采样率现在会**记住**,下次打开还是上次的样子
- 曲线缓存改为**最多保留最近 48 小时**并自动丢弃更旧的点,连开几周也不会把内存吃满(CSV 记录始终完整)
- 界面上的"模拟"一律改叫"虚拟",避免和"模拟量/analog"混淆

### v0.6

- 通道设置对话框顶部显示该路的实时温度和传感器原始值
- 连接设置重排为温度测量 / 直流电源 / 精密电源三个区域;新增 Lake Shore 325 与 Keithley 2450
- 变化率数字按升温 / 降温染成红色和蓝色
- 新增"测试结果"按钮与独立的 `*_result.csv`:每轮制冷的最低温度和保持时间
- 常温下也能点"开始测试",状态栏会显示"正在等待预冷完成"

## 连接助手

### v0.3.1

- 只允许开一个实例,再双击会把已经开着的窗口调到前面

### v0.3.0

- 按仪器清单逐台显示指示灯,可以直接添加/删除仪器
- 向导去掉"选型号"那一步(型号在添加时就定了),只列这台真正支持的连接方式
- 填地址时检查有没有和清单里别的仪器撞车

### v0.2.2

- 菜单里加上使用说明(<kbd>F1</kbd>),默认翻到"连接仪器"那一章
- 修正任务栏上不显示软件图标的问题

### v0.2.1

- 窗口按屏幕可用高度自动收窄,笔记本上不会超出屏幕

### v0.2

- 连接失败时按报错自动判断问题出在哪一头:是电脑网卡没设好,还是仪器那边的问题。
  属于电脑网络的问题时直接把"设置电脑网卡"做成主按钮,并列出各网卡当前的 IP,
  不再一股脑列出仪器面板的排查步骤
- 两台测温仪配置成同一个 IP 时会提醒 —— 撞 IP 的表现是时好时坏,很难查

### v0.1

- 三个指示灯显示温控仪、直流电源、源表的连接状态
- 分步向导:选型号 → 选连接方式 → 填参数 → 自动检测
- 连不上时给出该仪器的设置步骤,以太网仪器可直接修改电脑网卡 IP
- 一键恢复网络设置,联网检查更新
